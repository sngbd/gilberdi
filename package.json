{
  "scripts": {
    "clean": "rimraf output && rimraf .spago",
    "build": "spago build && spago bundle-app --to public/output/main.js",
    "serve": "http-server ./public -p 3000"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^4.0.12"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "purescript": "^0.15.10",
    "rimraf": "^5.0.5",
    "spago": "^0.21.0"
  }
}
